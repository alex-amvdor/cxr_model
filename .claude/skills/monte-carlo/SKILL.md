# Monte Carlo implementation rules

- Preserve RNG reproducibility.
- New stochastic processes require regression tests.
- Always benchmark new kernels.
- Keep CPU and GPU implementations numerically consistent.
